services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: dgg-frontpage:latest
    restart: unless-stopped
    environment:
      NODE_ENV: production
      NEXT_TELEMETRY_DISABLED: "1"
      PORT: "3000"
      HOSTNAME: 0.0.0.0
    ports:
      - "3000:3000"
